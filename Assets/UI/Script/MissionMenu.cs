using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MissionMenu : UIPanel
{
    // Start is called before the first frame update

    protected override void Start()
    {
        level=1;
        canCover=true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
